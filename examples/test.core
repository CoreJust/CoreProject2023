import core.io.console;
import core.crt.cstdlib;

use console;

struct String {
	c8* data;
	u64 capacity;
	u64 size;
}

def main() i32 {
	String str;
	str.data = "Hello world";
	console.println(str.data);
	
	cstdlib.lldiv_t div = cstdlib.div(18i64, 5);
	
	console.print(div.quot);
	console.print(" : ");
	console.println(div.rem);
	
	cstdlib.system("pause");
	return 0;
}