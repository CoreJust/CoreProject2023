# The module that is imported by default
# Implements (directly or through import) some of the basic core language features
@set visibility public
@set safety safe
@set default_imports false
import core.math.basic_operators;