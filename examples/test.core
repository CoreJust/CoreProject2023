import some_file;

i32 g_printer = 40;

def main() i32 {
	some_file.putchar(g_printer);
	g_printer = 45;
	some_file.putchar(g_printer);
	var a = 10;
	some_file.putchar(a);
	a = 90;
	some_file.putchar(a);
	
	some_file.getchar();
	return 0;
}