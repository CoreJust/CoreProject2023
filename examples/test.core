import core.io.console;

use console;

def arrTest(u8 val) u8[10] {
	return u8[10] { val, 2*val, 3*val, 4*val };
}

def main() i32 {
	str8 s = "sss";
	println(s[2]);

	u8[] a = arrTest(15);
	u64 i = 0;
	while i < a.size println(a[i++]);
	
	console.pause();
	return 0;
}