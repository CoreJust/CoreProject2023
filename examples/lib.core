def native putchar(i32 val) i32;
def native getchar() i32;

def native puts(const c8* str) i32;

def native itoa(i32 value, c8* str, i32 base ) c8*;

def native strcpy(c8* dest, const c8* src) c8*;

def native malloc(u64 size) u8*;

c8* g_buffer = c8*(malloc(24u64));

def print_str(str8 str) i32 = puts(c8*(str));
def print_int(i32 i) {
	puts(itoa(i, g_buffer, 10));
}