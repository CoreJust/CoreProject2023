import core.crt.cstdio;
import core.crt.cstdlib;

def print(str8 str) i32 = print(c8*(str));
def print(c8* str) i32 = cstdio.puts(str);
def print(i8 i) i32 = cstdio.printf(c8*("%d\n"), i32(i));

def main() i32 {
	print("some string");
	print(10);
	print<i8>(15);
	
	cstdlib.system(c8*("pause"));
	return 0;
}