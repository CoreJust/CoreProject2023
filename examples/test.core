import lib;

c8* g_buffer = c8*(lib.malloc(24u64));

def print_str(str8 str) i32 = lib.puts(c8*(str));
def print_int(i32 i) {
	lib.puts(lib.itoa(i, g_buffer, 10));
}

def fn(i32 val) {
	if val && val - 5 print_str("true\n");
	elif 1 - bool(val - 5) print_str("secret\n\a");
	else print_str("false\n");
}

def main() i32 {
	i32 a = bool(0 - 5);
	i32 a = bool(0);
	i32 a = bool(1);
	fn(90);
	fn(0);
	fn(5);
	
	lib.getchar();
	return 0;
}