import core.crt.cstdio;
import core.crt.cstdlib;

def putch(c8 c) i32 = cstdio.putchar(i32(c));

def main() i32 {
	c8* str = c8*("some string");
	putch(*(str++));
	putch(*(++str));
	putch(*(--str));
	
	cstdlib.system(c8*("pause"));
	return 0;
}