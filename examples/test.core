import core.io.console;
import core.crt.cstdlib;

def test(i64 a, i64 b, i64 c) bool = a < b < c;

def main() i32 {
	console.println(test(1, 2, 3));
	console.println(test(1, 5, 3));
	
	cstdlib.system(c8*("pause"));
	return 0;
}