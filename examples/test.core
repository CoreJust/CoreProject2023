import core.io.console;

use console;

def main() i32 {
	str8 s = "abcdefgh";
	u64 i = 0;
	
	println("Do-While: ");
	do println(s[i]);
	while ++i < s.size;
	
	println("\nFor: ");
	for u64 j = 0; j < s.size; println(s[j++]);
	
	console.pause();
	return 0;
}